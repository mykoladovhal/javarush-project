-- Запит для виведення повних імен співробітників у верхньому регістрі
-- Використовуємо CONCAT для об'єднання імені та прізвища з пробілом
-- Перетворюємо результат у верхній регістр за допомогою UPPER
select upper(concat(first_name, ' ', last_name)) as full_name_upper
from employees